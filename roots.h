#ifndef ROOTS_H
#define ROOTS_H

extern int roots(unsigned char [256], unsigned char [528], int *, unsigned char *);
extern int roots_same(unsigned char *, unsigned char *);
extern int roots_init(char *);

#endif
